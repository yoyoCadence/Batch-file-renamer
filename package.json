{
  "name": "batch-file-renamer-pwa",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "test:e2e": "playwright test",
    "build": "npm run test"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1"
  }
}
